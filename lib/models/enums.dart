enum Clarity { clear, slightlyCloudy, cloudy }

enum Brightness { dull, bright, brilliant }

enum Concentration { pale, medium, deep }

enum Color { straw, yellow, gold, purple, ruby, garnet }

enum Hue { silver, green, orange, blue, ruby, garnet, brown }

enum Staining { none, light, medium, heavy }

enum Tears { light, medium, heavy }

enum Fault { tca, h2s, va, ea, brett, oxidation }

enum Intensity { delicate, moderate, powerful }

enum AgeAssessment { youthful, vinous }

enum Fruit {
  apple,
  citrus,
  stone,
  tropical,
  melon,
  redBerry,
  blackBerry,
  blueBerry
}

enum FruitCharacter { baked, stewed, dried, peelSkin, tart, ripe, jammy }

enum NonFruit {
  floral,
  vegetal,
  herbal,
  spice,
  vinification,
  chocolate,
  coffee,
  liquorice,
  olive
}

enum OrganicEarth { forestFloor, compost, mushrooms, freshSoil, farmYard }

enum InorganicEarth { mineral, limestone, chalk, slate, flint, volcanic }

enum TertiaryAged { meaty, leather, truffle, dusty }

enum Sweetness { boneDry, dry, offDry, sweet, verySweet }

enum IntensityScale { low, mediumMinus, medium, mediumPlus, high }

enum BodyTexture { light, medium, full, creamy, round, lean }

enum Wood { old, newWood, large, small, french, american }

enum Length { short, mediumMinus, medium, mediumPlus, long }

enum Complexity { low, moderate, complex }

enum WineType { red, white }

enum OldWorldNewWorld { oldWorld, newWorld }

enum Climate { cool, moderate, warm }

enum AgeRange { under3, under5, under10, over10 }

enum Quality { aocDocg, grandPremierCru, reserva, granReserva }
